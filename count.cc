#include "count.h"

void count_number() {
    // lab2 //
    // do it yourself //
    using namespace std;
    int count;
    while (count < 11)
    {
	    cout << count;
	    cout << "\n";
    }

    cout << "2019312742";
    cout << endl;

    return 0;
}
